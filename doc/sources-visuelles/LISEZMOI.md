# Sources visuelles — hors publication

Ces fichiers ne sont **pas** servis par le site : ils sont ici pour être
retravaillés, pas publiés.

- `logo-complet.png` (1200×1200) et `logo-master.webp` (1000×1000) :
  déplacés de `public/` le 30/07/2026 par l'audit SEO. Ils y étaient
  publiés — un demi-mégaoctet téléchargeable — alors qu'aucune page, ni
  le code, ni le contenu ne les référençait.
- `logo-800-original.png` : l'original de `public/logo.png` avant sa
  réduction à 512 px pour les données structurées (Google demande au
  moins 112 px de côté ; 800 px en PNG non optimisé pesait 281 Ko).

Pour republier l'un d'eux : le remettre dans `public/` ET le référencer
quelque part, sinon l'audit le signalera de nouveau.
